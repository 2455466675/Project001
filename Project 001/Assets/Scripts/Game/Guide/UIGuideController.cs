using UnityEngine;

namespace Game.UI
{
    /// <summary>
    /// 
    /// </summary>
    public class UIGuideController : MonoBehaviour
    {
        private void OnSelectGuidableChangedHandler()
        {
            //Vector2 screenV2 = RectTransformUtility.WorldToScreenPoint(camera, guidableItem.GuidePoint());
            //RectTransformUtility.ScreenPointToLocalPointInRectangle(transform as RectTransform, screenV2, GameCore.UI.UICamera, out Vector2 targetV2);
        }
    }
}